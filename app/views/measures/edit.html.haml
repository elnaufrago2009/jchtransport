- page_title!('Editando measure')

#region-measures.container

  .region
    .region-title
      %h1
        Editando
        %b
          measure

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @measure, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
