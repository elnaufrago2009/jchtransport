- page_title!('Editando guide')

#region-guides.container

  .region
    .region-title
      %h1
        Editando
        %b
          guide

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @guide, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
