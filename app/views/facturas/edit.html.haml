- page_title!('Editando factura')

#region-facturas
  .region
    .page-header
      %h1
        %span.glyphicon.glyphicon-th
        Editando
        %b
          Factura
        %small
          (
          = @factura.numero
          )

  .region
    = render 'form'

